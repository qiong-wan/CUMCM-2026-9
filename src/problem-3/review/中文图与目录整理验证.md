# 中文图与目录整理验证

日期：2026-09-11。本次只修改图中文字、字体设置、图像保存位置及输出文件的读写路径。未按代码审查报告调整方程、参数、随机模型、迭代、事件定位或数值验收规则。

## 1. 目录与数据保留

参照前两问，正式结果表保留在 output/problem-3/ 根目录，科学图归 image/，验证证据归 review/。27 个案例文件归 cases/，5 个派生数据和中间载荷文件归 data/。模型推导、代码审查、分类及本说明均保留在 src/problem-3/。

本次迁移 51 个文件，移动前核对绝对路径均在第三问输出目录内且目标不存在，仅清理迁移后空 previews/ 目录。数值基线比较覆盖原根目录全部 42 个 NPZ、JSON、CSV 和 XLSX，包含主解、加密案例、事件起点、导出数据、验证与保护记录；所有 SHA-256 均一致。

数值源码基线覆盖 11 个组件：properties、Solver、Environment、plateau_fluctuation_stats、simulate、refine_event、compare、event_checks、integrate_local、synthetic_tests、boundary_metrics。按 AST 比较全部一致。原审查报告保留当时的路径和缺陷证据，应结合本次目录调整理解，不将本次呈现检查视为完整数值或随机模型验收。

## 2. 中文图像

三个绘图入口 deliver_problem3.py、generate_figures.py、ensemble_problem3.py 共用 plot_style.py。图内标题、坐标轴、图例和时间说明改为中文，保留 cm、kg/kg、°C 等单位以及 N 等数学变量。实际选用 Microsoft YaHei；SVG 嵌入字形，避免换机查看缺字。

已用现有验证后的确定性数据执行独立绘图入口，生成四组 SVG/PNG，并从同一 Figure 直接输出 JPEG 核查图。人工查看的是以下 JPEG，没有读取 output 下的 PNG。

| 图像 | 中文排版检查 |
|---|---|
| [干燥历程](../../../output/problem-3/image/previews/drying_history.jpg) | PASS：双子图标题、图例、阈值和轴文字完整 |
| [径向分布](../../../output/problem-3/image/previews/radial_profiles.jpg) | PASS：时间图例、径向距离、局部表面分布标签完整 |
| [温度与环境](../../../output/problem-3/image/previews/temperature_and_environment.jpg) | PASS：温度、含水率单位及末值/均值图例完整 |
| [收敛](../../../output/problem-3/image/previews/convergence.jpg) | PASS：空间/时间加密标题、轴单位及科学计数刻度清晰 |

自动渲染比较覆盖新旧正式绘图入口、每种子的随机环境图、随机路径图和时间分布图。对比翻译前后的全部曲线横纵坐标、坐标范围及直方图几何，全部一致；缺字警告作为错误处理，未发现缺字或未翻译的英文文字。随机图只在内存中使用明确的渲染测试输入，不运行随机集合，也不生成或发布虚构的随机数值成果。

## 3. 实际检查与限制

| 检查 | 结果 |
|---|---|
| 42 个数值文件哈希 | PASS，全部一致 |
| 11 个核心数值组件 AST | PASS，全部一致 |
| 5 组绘图入口/分支比较 | PASS，曲线及几何一致，中文无缺字 |
| 四幅正式中文图生成与 JPEG 视觉检查 | PASS |
| 新旧案例读取器 | PASS，逐个读取 9 个案例，所有数组和元数据一致 |
| 新旧工作簿读取器 | PASS，两者均逐格检查 72051 个含水率格，最大差 0，格式错误 0，表 5 的 50 个对应值一致 |
| 续跑路径 | PASS，识别全部 9 个案例；隔离控制流检查，不执行被模拟的求解子进程 |
| Python/Node 语法及本地文档链接 | PASS，10 个 Python 文件、Node --check、68 个本地链接通过 |
| 完整求解、空间/时间/事件重新验证 | SKIPPED，保留原数值成果，不改变求解逻辑 |
| 完整随机集合与随机误差验证 | SKIPPED，本次只检查渲染 |
| 工作簿重新导出与 PNG 视觉重查 | SKIPPED，现有工作簿和三张工作簿预览按原样保留 |
| 原代码审查故障探针重跑 | SKIPPED，仅同步探针所需路径，不覆盖原审查记录 |

机器记录：[presentation_checks.json](../../../output/problem-3/review/presentation_checks.json)、[layout_checks.json](../../../output/problem-3/review/layout_checks.json)。当前路径索引见[文件分类与用途](../文件分类与用途.md)。

## 4. 复现本次检查

在项目根目录执行：

```powershell
python -B -X utf8 src/problem-3/generate_figures.py --require-full --no-stochastic
python -B -X utf8 src/problem-3/review/verify_presentation.py
python -B -X utf8 src/problem-3/review/verify_layout.py
node --check src/problem-3/build_workbook.mjs
```

presentation_before.json 是整理前固定基线，已有基线不能通过 --capture 覆盖。以后有意改变数值结果或算法时，旧基线检查应报告差异，不得据此直接覆盖基线以取得 PASS。
