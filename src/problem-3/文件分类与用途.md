# 第三问文件分类与用途

参照问题 1、2：结果表保留在 output 根目录，科学图放入 image/，检查数据放入 review/；第三问额外的多网格案例放入 cases/，派生数据和导出载荷放入 data/。代码、模型推导、代码审查和使用说明保留在 src/problem-3/。

本次只调整中文呈现、输出位置和相关读取路径。现有确定性结果、数值算法和参数保持不变，随机模型的审查问题仍按原报告理解。原审查报告中的旧目录描述属于审查时的历史证据。

## 1. 先按任务找文件

| 任务 | 文件 |
|---|---|
| 最终结论、误差与环境假设 | [结果与验证.md](../../output/problem-3/结果与验证.md) |
| 题目工作簿 | [result3.xlsx](../../output/problem-3/result3.xlsx) |
| 表 5 | [table5.md](../../output/problem-3/table5.md)、[完整精度 CSV](../../output/problem-3/table5_moisture.csv) |
| 方程、假设与离散推导 | [模型与算法说明.md](模型与算法说明.md) |
| 当前代码不足与历史复现证据 | [代码审查报告](问题3_代码审查报告_GPT.md) |
| 运行命令、依赖和数组含义 | [README.md](README.md) |
| 本次中文化与分类检查 | [整理验证说明](review/中文图与目录整理验证.md) |

## 2. 当前输出布局

以下路径相对于 `output/problem-3/`。

```text
problem-3/
  README.md
  result3.xlsx
  table5.md
  table5_moisture.csv
  moisture_60s_full_precision.csv
  结果与验证.md
  image/
    drying_history.svg / .png
    radial_profiles.svg / .png
    temperature_and_environment.svg / .png
    convergence.svg / .png
    previews/               工作簿预览、科学图 JPEG 核查图
    stochastic/             随机样本路径和分布图（有数据时）
  cases/                    9 个计算案例，共 27 个文件
  data/                     观测副本、派生边界、载荷与环境记录
  review/                   数值验证、审查测试、哈希与导出检查
  stochastic/               随机试算的数据、集合结果和报告（有数据时）
```

根目录的 XLSX、CSV、表 5 和报告共同来源于 `cases/production.npz`。图名保留稳定英文文件名，图内标题、轴名、图例及文字标注采用中文；物理单位和数学变量保留标准符号。图内数据、颜色、曲线选择和坐标范围不因翻译改变。

## 3. src：代码与说明

以下路径相对于 `src/problem-3/`。

| 文件 | 作用 |
|---|---|
| `solve_problem3.py` | 只读审计、局部物性、有限体积、隐式积分、事件定位；统一输出目录常量；合并验证与交付入口 |
| `verify_problem3.py` | 独立数值验证与工作簿逐格复核 |
| `deliver_problem3.py` | 从已有解生成表 5、CSV、载荷、图表和结果报告 |
| `build_workbook.mjs` | 从原始模板与 data/workbook_payload.json 导出根目录 result3.xlsx |
| `run_all.py` | 旧版完整流程；续跑检查 cases/ 中的案例 |
| `generate_figures.py` | 独立重绘科学图和实际存在的随机种子环境图 |
| `ensemble_problem3.py` | 随机后续环境试算；数值文件写 stochastic/，图像写 image/stochastic/ |
| `plot_style.py` | 中文字体选择、缺字报错、SVG 字形嵌入 |
| `README.md`、`文件分类与用途.md` | 复现说明与本索引 |
| `模型与算法说明.md` | 初边值问题、完整推导、算法、参数及局限 |
| `问题3_代码审查报告_GPT.md` | 原代码审查发现及证据，保留历史结论 |
| `review/review_problem3.py` | 短时数值对照与隔离故障探针，已同步读取路径 |
| `review/verify_presentation.py` | 中文渲染、绘图几何数据、数值文件哈希和数值代码回归 |
| `review/verify_layout.py` | 目录契约、案例与工作簿读取器、续跑控制流、语法及文档链接检查 |
| `review/visual_verification.md` | 原视觉审查记录及当前图片归档位置 |
| `review/中文图与目录整理验证.md` | 本次实际执行检查及未执行项 |
| `.gitignore`、`node_modules/` | 忽略配置和已有依赖联接，不是计算输出 |

## 4. cases：完整精度数值解

### 4.1 同一案例的三种文件

每个案例以名称为前缀，通常保留下面三个文件；它们承担不同职责。

| 文件形式 | 内容 | 作用 |
|---|---|---|
| `<案例名>.npz` | 未舍入温度和含水率数组、21 点输出、所存时刻的细网格全场、结束状态与累计诊断 | 再分析、比较及生成成果的数值来源；并非保存了每个内部时间步的所有场 |
| `<案例名>.json` | 网格、时间倍率、环境模式、事件夹逼、步数、迭代和守恒诊断 | 快速查看一次求解的参数和结果；不能单独替代完整验证报告 |
| `<案例名>_event_seed.npz` | 跨越阈值前的完整状态、起点时间和搜索宽度 | 对末期事件进行局部重算和精度检查，不能当成交付终态 |

这些 NPZ 同时保留温度与水分，因为温度参与扩散系数计算和热平衡检查；正式工作簿仍只导出水分。数组含义详见源目录 README 的“NPZ 数组约定”。部分研究案例 JSON 中的 `PENDING_CONVERGENCE` 是单次求解留下的状态，整套已执行检查以 `review/verification.json` 的分项记录为准。

### 4.2 各案例具体用途

N 表示径向区间数，实际节点数为 N+1；factor 是内部步长计划的倍率，不是输出间隔。last 表示观测结束后同时保持温度和环境水分末值；mean30 表示保持 12600 至 14400 s（含两端、31 个观测）的末段算术均值。两者均保留观测区间的原始分段线性边界。

| 案例前缀 | N | factor | 后续环境 | 定位及是否为完整流程必需 |
|---|---:|---:|---|---|
| `production` | 12800 | 0.5 | last | **正式主解**；必需，是表 5、工作簿和主结论的共同来源 |
| `space400` | 400 | 1 | last | 空间加密第 1 级；必需 |
| `space800` | 800 | 1 | last | 空间加密第 2 级；必需 |
| `space1600` | 1600 | 1 | last | 空间加密第 3 级；必需 |
| `space3200` | 3200 | 1 | last | 空间加密第 4 级；必需 |
| `space6400` | 6400 | 1 | last | 空间加密第 5 级；必需 |
| `space12800` | 12800 | 1 | last | 空间加密第 6 级兼时间加密基准；必需 |
| `time12800quarter` | 12800 | 0.25 | last | 与 space12800、production 组成独立时间加密序列；必需 |
| `mean12800` | 12800 | 0.5 | mean30 | 与 production 同网格和时间设置的正式边界敏感性对照；必需 |

上述 9 个前缀各对应三类文件，共 27 个，均保留以支持正式流程和复核。两组不参与当前正式流程的早期试算已删除，详见第 8 节。


## 5. data：派生数据和中间载荷

| 文件 | 作用与限制 |
|---|---|
| `input_audit.json` | 原始字段、单位、有限性、缺失、重复、时间顺序与间隔的只读审计 |
| `observations_readonly_copy.csv` | 只读解析后的观测副本；权威输入仍是原附件 |
| `derived_boundaries.csv` | 观测插值及末值、末段均值延拓，未替换原始数据 |
| `workbook_payload.json` | 主解生成的完整精度导出载荷，可重新生成 |
| `run_environment.json` | 原计算的软件版本，整理时保留原记录 |

## 6. review：验证与溯源

| 文件 | 作用 |
|---|---|
| `verification.json` | 现有完整数值检查的原始记录，不是本次重新计算的结果 |
| `workbook_verification.json` | 表头、时间、距离、72051 个含水率格、格式与表 5 的逐点复核 |
| `near_end_common_fields.npz` | 共同末期时刻的加密全场对照 |
| `problem2_comparison_snapshot.npz` | 第二问对照数据快照，不作为续算初值 |
| `protected_hashes_before.json`、`protected_hashes_after.json` | 原计算的保护文件哈希基线与之后记录 |
| `verification_initial_scope_failure.json` | 首次保护检查失败的历史记录 |
| `review_checks.json`、`probe_stdout.txt` | 初次代码审查的原始证据 |
| `review_checks_current.json`、`probe_stdout_current.txt` | 前次文档归位后的审查探针结果，本次未重跑 |
| `presentation_before.json` | 本次整理前的源码、42 个数值文件哈希及 11 个核心组件基线 |
| `presentation_checks.json` | 本次图像和数值保留检查的实际结果 |
| `layout_checks.json` | 目录契约、两个工作簿读取器、续跑路径与链接检查 |
| `artifact_inspection.txt` | 工作簿导出工具的简要结构检查 |
| `.mplconfig/` | 新绘图入口可重建的本机字体缓存，不属于正式交付 |

数值记录中的 PASS、FAIL、SKIPPED 保留原含义；代码审查记录与正式数值验收不是同一检查。本次呈现检查 PASS 不代表随机模型和完整求解流程已重新验收。

## 7. image：科学图与预览

四幅正式科学图提供 SVG 和 PNG。SVG 嵌入中文字形。主图为干燥历程、径向含水率、温度与环境、空间和时间收敛；各文件名见第 2 节。

`image/previews/` 保存三张原工作簿/模板 PNG 预览，以及四张直接由绘图 Figure 输出的 JPEG 核查图。科学图原 `previews/*_qa.png` 已迁移为 `image/*.png` 并重新生成中文版本。本次只查看 JPEG，没有读取 output 下的 PNG。

随机数据实际存在时，`generate_figures.py` 沿用原规则在 image/ 生成每个种子的环境图；集合入口在 image/stochastic/ 生成路径图与临界时间分布。当前没有实际集合结果，本次仅用内存测试数据检查随机绘图，不发布模拟结果。

## 8. 整理与保留记录

本次迁移 51 个文件，所有目标均事先检查不存在，完成后仅删除空 previews/ 目录。42 个数值文件逐字节保持一致，模型推导与审查文档仍在 src。4 幅科学图在原数据上改为中文。旧求解器、新合并入口、绘图器及 JS 导出器均按第 2 节读取和写出。

以下是上次已完成的冗余文件清理记录，不代表本次又执行了删除：

| 已删除文件 | 数量 | 删除依据 |
|---|---:|---|
| `src/problem-3/problem_page2.png`、`problem_page4.png` | 2 | 仅用于读题的临时页面截图，可从未改动的原 PDF 再生成 |
| `output/problem-3/time6400half.npz`、`time6400half.json`、`time6400half_event_seed.npz` | 3 | 早期时间试算，正式验证已使用 N=12800 的独立时间加密序列 |
| `output/problem-3/mean6400.npz`、`mean6400.json`、`mean6400_event_seed.npz` | 3 | 早期边界试算，正式敏感性对照使用 mean12800 |
| `output/problem-3/result3.xlsx.inspect.ndjson` | 1 | 自动导出诊断冗长，正式工作簿、简要诊断和独立逐格验证已保留 |
| `output/problem-3/.mplconfig/fontlist-v390.json` 及其空目录 | 1 个文件 | 可重建的旧字体缓存，后续改用进程临时缓存 |

合计清理 10 个文件，原占用 34,900,087 字节（约 33.28 MiB）。没有删除正式主解、必要加密案例、原始输入、数值验证记录或失败审计历史。
