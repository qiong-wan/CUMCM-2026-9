# A 题第四问完整求解提示词

请调用 `math-modeling-standards` 数学建模 skill，在 `D:\CUMCM\Solution` 中，依据已完成的第四问推导，实现并实际完成第四问的求解、验证、结果导出、科学绘图和中文说明。不要只给方案、伪代码或未经执行的程序。先阅读本项目适用的 `AGENTS.md`，遵守原始数据不可修改的要求。

本次使用以下明确分工：**除绘图外，所有第四问代码集中在 `solve_problem4.py`；全部绘图代码集中在 `generate_figures.py`。** 完成必要检查后再发布正式结果，不得伪造烘干时长、验证状态、图像或表格。

## 一、必读材料与任务边界

按以下顺序读取当前文件，不把历史审查结论直接当作当前源码状态：

1. `D:\CUMCM\Solution\problem.pdf` 中第四问、表 6、附录 4，以及前三问和附录 2、3 的相关要求。
2. `D:\CUMCM\Solution\src\problem-4\问题4_解题框架与公式推导.md`，全文共 110 个编号公式，是本问的主要数学依据。
3. `D:\CUMCM\Solution\src\problem-1\问题1_算法与模型说明.md`。
4. `D:\CUMCM\Solution\src\problem-2\问题2_模型与算法说明.md` 及求解代码。
5. `D:\CUMCM\Solution\src\problem-3\模型与算法说明.md`、求解代码、当前审查及整理记录。
6. `D:\CUMCM\Solution\output\problem-3\结果与验证.md`，只用于理解第三问已经报告的结果和验证范围。
7. `D:\CUMCM\Solution\data\附件1.xlsx`、`附件2.xlsx` 以及 `附件3\result4.xlsx` 原始模板。

本次目标是采用第四问主模型得到烘干时长、表 6、完整 `result4.xlsx`、图像和可复核证据。前三问只提供数学及算法参考，不作为第四问的续算初值。不能用第三问约 57.1695 h 的历史结果缩放或反推第四问答案。

保留现有推导文件。实现说明另写文件，不将“仅推导”的历史文档改成声称已经运行过求解的记录。不得顺手修复前三问、修改公共模块、覆盖旧结果或进行无关 Git 操作。

## 二、严格的两脚本结构

仅新增以下两个代码文件，位于 `D:\CUMCM\Solution\src\problem-4\`：

| 脚本 | 必须包含的职责 |
|---|---|
| `solve_problem4.py` | 数据读取与审计、单位换算、环境与半径函数、物性、网格与通量、热湿耦合、时间推进、事件定位、守恒与收敛验证、对照情景、运行记录、数值数据保存、表 6 和 Excel 导出及重读校验 |
| `generate_figures.py` | 读取已保存数值结果及元数据、绘图需要的重构、中文字体与风格设置、全部图像生成、图像清单与图像检查 |

允许使用函数、类和标准第三方库组织两份脚本，要求 Python 3.11+、PEP 8、函数有 docstring、路径基于脚本位置解析、导入模块不会启动求解或写文件。

不得增加第三份求解、审计、验证、导出、绘图样式或运行入口脚本；不得生成或调用临时自定义脚本来转移实现；不得把逻辑藏到 Notebook、JS、Shell、`src/common` 或前三问模块。已有算法可阅读并在本脚本内按需实现，不能导入前三问求解器充当第四问实现。

可以正常使用 NumPy、SciPy、openpyxl 和 Matplotlib 等第三方库。工作簿读取、模板复制、写出和校验均由 `solve_problem4.py` 内的 Python 函数完成，可使用 openpyxl，不另建 `build_workbook.mjs`。绘图脚本只依赖已保存数据，不通过导入或子进程重新积分 PDE，也不修改计算结果。

审计与测试函数也必须放在 `solve_problem4.py`，通过命令行选项调用。Markdown 文档、CSV、JSON、NPZ 等数据与说明文件不受“两份代码文件”限制。

## 三、输入审计和假设

原始题面、附件、模板只读。运行前后记录其 SHA-256，所有派生数据写入第四问输出目录。检查工作表、字段、单位、记录数量、数值有限性、缺失、重复时间、时间顺序、间隔和半径正值等。发现真正异常时先报告位置、证据与处理选项，未经用户批准不得删除、填补、纠正、合并、平滑或排除。可继续不依赖该异常处理的只读分析与实现工作。

历史审计记录仅作核对线索：附件 1 为 241 条观测，0 至 14400 s，每 60 s 一条；附件 2 为 145 条观测，0 至 259200 s，每 1800 s 一条，半径从 2 cm 变至 1.198 cm。必须重新核对当前文件，不能用这些摘要代替读取。不同时间的相同半径是有效记录；模板空格和省略号是占位符。

主方案明确采用：

- 同一初始均匀药材，初始温度 28°C、干基含水率 2.55 kg/kg、初始半径 0.02 m、长度 0.25 m。
- 一维径向轴对称，长度不变，骨架均匀径向收缩，初始干物质密度均匀，表面随骨架运动。
- 附件 2 的半径 cm 在独立对象中换成 m，按原始时间点分段线性插值，不拟合或平滑原始半径。
- 附件 1 的温湿环境在观测内分段线性插值，14400 s 后保持末次实测温度与环境含水率。该延拓是明确假设，观测结束不代表工艺阶段已被识别。
- 附录 4 全时段使用，表面对流系数继承附录 2。物性取局部药材状态。
- 采用推导中的经验显热和有效传质闭合，不自行加入潜热、湿分携带焓、压缩功或收缩拟合系数。

半径函数默认禁止超出 72 h 后静默外推。若到 72 h 未达标，应保存已完成的场和验证记录，报告数据覆盖内未达标，再提出后续半径情景供用户决定；不能返回 72 h 作为答案，不能认为药材永远无法烘干，也不能隐藏这一输入限制。

## 四、数学模型必须与推导一致

以下公式编号均指向既有 `问题4_解题框架与公式推导.md`，不在本提示词中重新编号。

1. 坐标和场使用公式（49）：固定计算域为 0 至 1，物理距离随当前半径变化；U 表示温度，X 表示干基含水率。
2. 局部物性逐字落实公式（15）至（19）；尤其扩散公式采用绝对温度，不能直接代入摄氏温度，不能替换为烘房温度。
3. 实现公式（57）、（58）的散度形式。温度与含水率在同一时间阶段共同确定，不冻结其中一场完成全程计算。
4. 按公式（59）至（61）实现初值、轴线对称和真实表面 Robin 条件，内部保留平方半径因子，表面保留半径因子。
5. 公式（25）至（36）、（49）至（53）已说明：干基比值消去几何浓缩项，均匀随体坐标下物质平流与网格运动项抵消。不能把公式（63）的另一种近似额外叠加到主模型。
6. 附录 4 的经验密度用于显热容量；不能同时强制将其除以一加局部含水率得到的量，当作满足均匀收缩连续性方程的干物质密度。保留推导第 10.1 节的解释限制。
7. 不把变物性移出散度，不把储存项替换成体积、热容和状态乘积的简单前后差。
8. 时间、空间和输出内部均保持未舍入值。非法状态或物性非正、非有限、下溢时明确拒绝该步或报错，不靠硬截断含水率或扩散系数让结果看似正常。

首先逐项核对推导与题面。若发现实质矛盾，明确指出证据和影响，不能悄悄更换物理模型；在目前已明确的有效模型假设下推进常规实现，无须为普通算法选择反复询问。

## 五、数值方法与达标事件

沿用推导公式（95）至（106）的节点中心有限体积思路：固定随体网格，中心半控制体、表面半控制体，界面调和平均，相邻控制体共享同一界面通量。区分计算网格与 0.1 cm 的实际输出网格。

时间推进优先选择适合长时刚性热湿耦合的隐式方法，可采用 SDIRK2；后向 Euler 可作为基线或独立时间对照。若选用高阶方法，应在方法文档补足实际阶段公式与推导。禁止构造巨大稠密矩阵；使用带状或稀疏线性代数。

每个隐式阶段都按实际阶段时刻计算半径、环境和最新局部物性。使用分块 Picard 或有依据的 Newton，同时检查两场尺度化迭代变化与原始非线性方程残差。合理减小失败步长，设置迭代与重试上限；达到上限必须报错并保留诊断。

启动阶段按初边值角点不相容处理，用足够小的步长解析早期表面薄层。时间推进要正确处理环境及半径插值节点；60 s 仅是输出间隔。选择网格、步长与收敛容差时，应由实际加密证据支持，不能直接复制第三问配置并宣布已收敛。

达标按公式（84）至（86）处理：

- 每个接受状态检查全计算域最大含水率，不仅检查中心、表面、平均值或少量交付节点。
- 找到首次阈值穿越的时间夹逼区间，并从保存的、未舍入的完整前状态进行局部事件细化。插值猜测不能代替对候选结束时刻的积分和全场核查。
- 没有验证单调性前，不预设中心一定最后达标；记录最大值的位置和径向趋势。
- 分开报告临界时间、严格达标结束时刻、含水率裕度、事件夹逼宽度和总数值误差。四位小数显示成 0.1500 不代表未舍入值恰好等于阈值。
- 不能把毫秒级根定位容差当成整体时长精度，也不能把数值严格达标裕度当成真实工艺安全余量。

## 六、必须实际执行的验证

所有验证入口和实现集中在 `solve_problem4.py`。先实施小规模有针对性的检查，再完成生产精度验证；检查失败后修复并重跑受影响部分，禁止只写“建议验证”。

至少包括：

1. 物性和单位检查：附录 4 的指定状态计算、绝对温度转换、非法状态拒绝。
2. 无收缩退化：半径恒定时退化为附录 4 固定几何模型；同时换回附录 3 才对应第三问，不能混淆两种退化。
3. 纯收缩不失水：关闭表面水分交换、初始含水率均匀时，即使半径收缩，干基含水率仍保持初值。
4. 均匀热平衡、中心零通量及内部共享通量相消；强制不收敛时能明确失败。
5. 水分独立平衡：按公式（81）、（102）比较全场平均变化与独立累计的表面通量，使用实际积分阶段权重；不能直接用状态差构造“边界累计”再自证守恒。
6. 显热独立平衡：按公式（83）累计变热容储存率和表面热输入；明确它验证的是选定经验显热方程。
7. 轴线和表面边界：独立重构梯度并检查 Robin 残差，注明单位和阈值；初始角点单独诊断。
8. 空间加密：使用不少于三级合理网格，分别比较完整场、实际输出点和临界时长。包括启动、典型 6 h 截面、表面薄层及终点附近。
9. 时间加密：固定空间网格，至少三级时间精度设置；不能同时改变网格和步长后把差异都称为空间误差。
10. 事件加密：收紧定位容差，并对事件附近局部积分做细化，区分定位与累计积分误差。
11. 用独立时间方法做代表性短时交叉核对，并说明若共享空间算子则不属于独立空间验证。
12. 工作簿逐格重读：数值、格式、时间、实际距离、域内掩码、表面值和终点行与同一已接受数据一致。

开始精度研究前写清验收目标、指标定义、单位与阈值依据。含水率四位小数交付可将相邻细化结果最大差小于 5e-5 kg/kg 设为初始目标，但必须报告实际覆盖范围和最坏差异，不能声称这一差值就是严格误差上界。还要独立控制并报告时长变化，说明末期含水率误差对时间误差的放大。没有达到目标时继续改进或明确报告未通过，不得为通过验收无依据放宽阈值。

每项输出 PASS、FAIL 或 SKIPPED 及证据；缺少必要检查不得提升为“全部验证通过”。主结果文件只有在完整前置检查通过后才可更新。失败案例、试算和正式结果分开保存，避免混合不同批次。

## 七、环境敏感性与第三问对照

在主解通过必要验证后，至少进行一次观测后环境的确定性敏感性检查：主方案使用末值，替代方案使用附件 1 最后 30 min 闭区间的温湿观测算术均值。说明窗口、样本数、均值定义和边界切换方式，不修改观测区间。

说明第四问时长与第三问的差异，但不能先验宣称收缩必然使烘干更快。引用推导公式（91）至（94），指出几何与附录物性同时改变。

为分辨两种影响，用同一求解脚本提供四个确定性对照：附录 3 固定半径、附录 3 实测收缩、附录 4 固定半径、附录 4 实测收缩。以相同环境、初值和明确的数值精度比较；附录 4 实测收缩为正式主方案。对照仍遵守 72 h 数据范围，未在范围内达标的情景如实记录，不强行制造完整时长排序。结果非线性，不能把效应差简单解释为普遍可加。

主方案与对照使用各自保存的元数据和边界。不要擅自增加随机环境集合；第三问旧结果或旧随机精度声明不能充当本问新验证。

## 八、结果数据与模板导出

正式输出位于 `D:\CUMCM\Solution\output\problem-4\`。保留清楚的组织结构：根目录放交付表格和结果说明，`data` 放派生输入和数据说明，`cases` 放主解及加密、对照案例，`review` 放验证记录，`image` 放图像。

必须保存可供后续复核和独立绘图的数据，包括：原始输入哈希、实际边界情景、半径历程、随体网格、未舍入温湿场、60 s 输出及终点、启动和 6 h 全场截面、表面和中心历程、全域最大值与位置、加权平均、通量累计、物性范围、事件夹逼及事件前完整状态。根据实际需要使用压缩数组或分批保存，避免把每个内部小步的整个细网格都复制成巨大工作簿。

必须支持读取已有案例并验证其输入、求解代码、参数、情景及数据结构指纹后再复用。不允许只依据文件名存在就跳过计算或把旧验证套用到新结果。绘图代码变化应与求解代码变化分开记录，避免重新绘图无故触发全程求解。

`result4.xlsx` 要求：

- 依据原模板保持 Sheet1、A1 和首行实际距离含义，不给提交工作簿增加温度工作表或其他说明工作表。
- 时间列单位为 s，从 60 s 开始每隔 60 s 一行；追加同一真实严格结束时刻，非规则终点在说明中写清，重合时不重复。
- 固定距离列按 0.1 cm 间隔展开，最后保留“药材表面”列。列集合覆盖本次输出中实际用到的规则内部距离，列含义不得随行移动。
- 根据推导公式（108）、（109）逐时刻映射当前实际距离；位于表面以内的规则点输出内部值，达到或超出当前表面的固定距离列留空。真实表面单独输出一次，禁止域外外推、填零或批量复制表面值。
- 写入未舍入数值，显示格式为 `0.0000`。空白掩码是该时刻的几何含义，不是待插补数据。
- 将每个输出时刻的实际半径单独保存至 `data/radius_history.csv` 并在报告中说明，确保“药材表面”位置可追溯。

表 6 同时保存为 `table6.md` 和 `table6_moisture.csv`：时间用 h，每 6 h 加同一结束行，固定实际距离每隔 0.5 cm，另列真实表面。与工作簿及未舍入源数组交叉核对，不能由显示值二次插值得到。

## 九、科学绘图

全部绘图、字体、配色和图像检查代码集中在 `generate_figures.py`。脚本读取已保存的已验证结果，执行后不改变案例数据、重新求解 PDE 或覆盖验证状态。图例、环境曲线及终点应来自对应案例元数据，不硬编码第三问环境标签或时长。

至少生成以下内容，可合理合并子图：

1. 原始温湿环境、派生延拓和实测半径变化，区分观测与假设区间。
2. 中心、表面、全域最大和干物质加权平均含水率历程，标出 0.15 阈值、临界及严格结束时刻。
3. 每 6 h 及结束时刻的含水率径向分布，横轴用实际 cm，各条曲线截止于自身真实表面。
4. 含水率时空分布，使用实际距离坐标并遮罩药材外部区域，画出真实收缩边界。
5. 中心、表面与环境温度历程，说明局部温度对扩散系数的作用。
6. 空间、时间加密和烘干时长收敛证据，数据必须来自实际完成的验证。
7. 通过验证的确定性情景对照和环境敏感性结果；超出覆盖范围未达标的情景明确标识。

若增加圆柱横截面图，要用真实半径、等比例坐标和可比色标；不得让不同半径的截面都占满相同圆盘而隐去收缩，也不得将一维径向模型画成已经求解二维轴向模型。

图中文字以中文为主，保留正确单位和数学符号；字体确实可用，无方框、缺字、重叠、截断。保持与前两问科学图风格一致，以信息表达为主，不加装饰性元素。

正式图片保存 SVG 和适合论文使用的高分辨率 PNG。遵守项目“不读取 output 下 PNG”的约束：可直接从同一 Figure 另存临时 JPEG 供视觉检查，不通过重新读取禁止的 PNG 生成预览。核查所有最终图的标签、单位、图例、表面边界和数据范围。保存图像清单，说明每图数据来源、情景、坐标和结论限制。

## 十、文档、命令行和最终验收

在第四问源码目录新增 `问题4_模型与算法说明.md`，详细说明实际执行的数据处理、假设、变量、物性、连续模型、离散方法、事件、容差、输出映射及代码对应关系。引用既有推导的准确公式号；补充新方法时给出完整推导，在新文档内连续编号，后续使用和结论明确引用式号。

在输出目录生成 `结果与验证.md`，包含真实时长、全域达标证据、表 6、网格和时间参数、守恒与边界残差、误差与敏感性、输入及代码指纹、依赖版本和未完成事项。数学建模 skill 要求的每个重要指标都需有定义、单位、来源、计算方式和解释；不能将经验模型数值平衡说成真实热力学完全闭合。

两脚本至少提供以下等价命令行能力，在项目根目录运行并实际检查：

```powershell
python -B -X utf8 src/problem-4/solve_problem4.py --audit
python -B -X utf8 src/problem-4/solve_problem4.py --self-test
python -B -X utf8 src/problem-4/solve_problem4.py --all
python -B -X utf8 src/problem-4/generate_figures.py --require-verified
```

`--all` 应完成主求解、必要验证、确定性对照及表格交付；提供合理的单案例、验证和恢复运行选项，避免每次都从头积分。绘图由第二个命令独立执行，不需要第三份运行入口脚本。

完成时确认：只有两份新增代码脚本；原始附件、模板、前三问和公共模块未被改写；模型与推导一致；正式交付前置检查齐全；表 6、工作簿、数值数组和图像来自一致的运行数据；没有未运行却标成通过的项目。将完整证据写入文件，最终回复遵守项目约定，只列交付文件和简短测试结果摘要。

现在开始执行。先核对题面与推导，再实施两个脚本和必要文档，实际运行检查，解决发现的问题，直到完成上述可验证交付；遇到原始异常或确实缺少必要模型输入时，明确说明证据和所需用户决定，不编造答案。
