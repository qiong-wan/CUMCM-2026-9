# 第四问图像视觉核查

核查日期：2026-09-12。状态：**PASS**。

## 本轮 04、06 图形修订

本轮仅重绘 04 和 06 两组图，并实际打开同一 Figure 直接导出的 JPEG 进行检查；未读取 PNG，未运行求解器。以下原批次其他图的核查记录保留，不能解释为本轮重新生成或再次逐张检查了所有图。

| 图名 | 本轮核查内容 | 结论 |
|---|---|---|
| 04_moisture_space_time | 0.15 kg/kg 白色虚线清晰可见，深色描边使图例可读；等值线位于真实药材区域内，从表面向中心推进；收缩边界、灰色域外区、坐标和色标保留 | PASS |
| 06_numerical_convergence | 空间、时间各显示三级真实存档结果，以本组最细解为参考；主方案网格和倍率以红圈区分；表格完整保留四组相邻场差、时长差及粗级未通过；文字无裁切和不合理遮挡 | PASS |

06 的最细级零值是自比较，不表示真实误差为零。图中空间组固定时间倍率 1，红圈只标出主方案采用的网格；时间组固定 N=51200。数据直接来自各级已验证事件元数据，没有把相邻场范数相加后伪造相对最细级的全场误差。

本轮前后核对的 17 个保护文件包括求解脚本、第三问绘图参考源码、原始输入、数值交付、验证记录和其余五张 SVG。绘图脚本还按本次读取范围校验数值文件的前后哈希；这些检查不代表重新求解或重新执行长时间数值验证。每张保留图继续记录原生成源码指纹，每张新图记录本轮源码指纹，见 figure_manifest.json。

实际结果：上述 17 个文件哈希均未变，脚本核对的 116 个输入和数值来源文件前后哈希一致。新图对应绘图源码 SHA-256 为 `48d7b30cc7cf9e3cf8d88ec759d4af2c9c2001535bba8f7f322e066f21ef4e1e`。两组收敛时长差重新从各自元数据交叉核对通过，0.15 等值线的所有顶点均在药材范围内。

论文选图、可新增的后处理图表及图注见 [论文图表展示建议](../../../src/problem-4/问题4_论文图表展示建议.md)。

## 原批次完整图像核查记录

已实际逐张打开 `D:/CUMCM/tmp/problem4_figure_previews` 中的七张科学图 JPEG 和两张工作簿 JPEG。所有 JPEG 均由 `generate_figures.py` 从同一 Matplotlib Figure 直接另存；本次未读取 `output` 下的 PNG。

## 科学图

| 图名 | 实际核查内容 | 结论 |
|---|---|---|
| 01_inputs_and_radius | 实测环境与 4 h 后末值假设以实线、虚线区分；半径为真实 cm，展示 72 h 观测覆盖及主方案结束时刻；单位和图例完整 | PASS |
| 02_moisture_history | 中心、表面、全域最大和干物质加权平均曲线正确标识；中心与全域最大重合是本次数值结果；0.15 阈值清楚；临界与严格结束时刻的 8 位小时显示可区分 | PASS |
| 03_actual_radius_profiles | 每 6 h 及结束时刻曲线在自身真实表面终止；各表面位置不同且可见；图例未遮挡曲线，横轴使用实际 cm | PASS |
| 04_moisture_space_time | 灰色为药材外部，色场止于实测收缩边界；半径从 2 cm 收缩到约 1.2 cm；色标、时轴和距离轴完整；未将径向场表述为二维轴向解 | PASS |
| 05_temperature_and_diffusion | 中心、表面和对应环境温度区分清晰；温度子图明确限定初期 0 至 6 h，扩散系数显示主方案全历程；扩散系数采用局部状态及 m²/s 单位 | PASS |
| 06_numerical_convergence | 六个子图分别展示空间、时间的含水率差、温度差和临界时长差；阈值线可见；粗级空间含水率差未达标的事实保留；对数刻度、图例和标题无截断 | PASS |
| 07_scenarios_and_sensitivity | 四种物性和几何组合标签正确；附录 4 固定半径明确标为 72 h 内未达标；均值环境相对末值的变化为约 +14.69 min；文本与图形无不合理重叠 | PASS |

采用实际可用的 Microsoft YaHei 字体。未见中文方框、缺字、标签截断或单位错误。图像脚本自动画布非空、可见文字边界和缺字检查也均通过，详见 `../image/figure_manifest.json`。实际绘图库版本、求解和绘图代码指纹、验证报告指纹及每张 SVG 的 SHA-256 均由该清单记录。

显示的小数位数不等同于模型精度。临界时刻和严格达标上端点的细微差别是离散事件定位；数值误差、模型假设及环境延拓敏感性分别见 `../结果与验证.md` 和模型说明。

## 工作簿预览

`workbook_part1.jpg` 与 `workbook_part2.jpg` 检查了保存后工作簿的选定行、全部距离列及表面列，包含初期、6 h、最后规则时刻和严格结束时刻。四位小数、非规则终点时间、固定距离列与域外空白均显示正常，文本在单元格范围内。

这些预览由保存后的数值和显示格式重构，不是原生 Excel 界面截屏；预览中的时间及距离单位用于便于阅读，正式工作簿的 A1 仍保持模板内容。完整数值一致性以 `delivery_checks.json` 的逐格回读为准：67100 格通过，20938 个几何空格通过，最大写回差 4.4408920985e-16；表 6 的 9 行也通过交叉校验。

## 交付与文件保护

`../delivery_manifest.json` 为 PASS；绘图前后数值文件哈希一致。最终复验区间内的 166 个受保护文件未变，见 `preservation.json`。原长时间运行期间其他题目目录的变化及原始失败快照另见 `实现调试记录.md` 和 `concurrent_changes_*`，不把本次复验通过解释为这些历史变化没有发生。
