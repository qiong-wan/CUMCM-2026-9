# 第四问实现调试记录

本文件保存被验收发现的问题及处理证据，不替代当前批次的 `verification.json`，也不将旧版本试算作为新版本的精度证明。

## 输入和基础模型

重新核对题面第 2 至 4 页、110 个编号的原推导及前三问当前源码与记录。原始观测未发现需要处理的异常。241 条环境观测和 145 条半径观测均保留，原始数据、模板及前三问未被改写。16 项初始物理与结构检查通过；后续新增实际重试案例的读取复用检查。

## 空间薄层

旧实现试算使用相同物理模型，从 N=1600 逐级加密。时长提前趋于稳定，但 0.01 s 的表面含水率仍存在较大网格差。N=12800 至 25600 的完整场最坏含水率差为 7.8816672893e-5 kg/kg；N=25600 至 51200 的最坏差为 1.9264745975e-5 kg/kg，才达到预设 5e-5 目标。正式版本仍重新执行自己的三级空间比较，旧数值仅为选取研究网格的线索。

## 4 h 切换处的时间精度

旧求解脚本 SHA-256 为 `6e0b4d33f78874027ff88c8b3c8b053461e42fe9e098e916fd66502482539d78`。固定 N=51200、时间倍率 1 至 0.5 的实际比较发现：

| 指标 | 旧版本差值 |
|---|---:|
| 最大温度场差 | 0.0013321842705238396 °C |
| 最坏温度时刻 | 14460 s |
| 最大含水率场差 | 2.0048838773600863e-6 kg/kg |
| 临界时长差 | 0.02105712890625 s |

虽然含水率和时长差较小，温度未达到预设 5e-5 °C 目标，因此这次时间验收为 FAIL，未生成正式 Excel 或正式图像。

原因是观测结束处名义步长由 4 s 突然放大到 60 s，没有充分解析环境导数改变引起的热瞬态；末 30 min 均值情景还会产生小幅边界跳变。修正为在 4 h 处重置启动斜坡，由小步长逐步增大，并额外保存切换后 0.01、0.1、1、10 s 的完整场。修正未改变物性、几何、连续方程、初值或环境观测。

## 复用与执行

初版文件保护扫描进入了第三问的外部 `node_modules` 链接，已改为只扫描项目自身文件并排除 output 下 PNG。中断尝试的目录保留；读取器现在也会核对完成的重试目录，避免完成重试后再次从头计算。该路径使用 N=16、0 至 60 s 的真实积分结果实际检验，恢复后数值文件按哈希核对。

每个进程限制 BLAS 为单线程并记录实际数值库版本和线程数。独立时间加密及对照案例可在同一求解脚本内用有界进程池执行，默认三个工作进程。小规模单线程试运行未显示可以直接宣称的显著提速，因此没有以性能假设替代实际运行。

修正后所有正式案例按完整新脚本指纹重新计算。基础检查和 4 h 切换的短时检查仅作为先行检查，最终是否通过以当前批次完整空间、时间、事件、守恒及情景验证为准。

修正后的先行检查采用 N=400、0 至 15060 s、末 30 min 均值延拓，时间倍率 1 与 0.5。实际比较 260 个共同完整场，最大温度差 5.3667527951e-6 °C，最大含水率差 2.0128789169e-6 kg/kg，两项均通过原目标。案例为 `transition_mean_f1-b9de649742d2b4d9` 和 `transition_mean_half-695c5aeb4f35b713`；这项检查未把短时截止 15060 s 解释为烘干时长。

## 修正后主方案的完整验证

当前求解脚本 SHA-256 为 `c61d4a4aa5de254af56374b84dfb92d2aebda0bc1cf51babf677987ec656b42f`。本批实际执行 N=12800、25600、51200 的空间序列，再在 N=51200 上执行时间倍率 1、0.5、0.25；全部比较覆盖 3058 个共同完整场。最细空间差为 C=1.9264745975e-5 kg/kg、T=1.0701022291e-8 °C，时长差绝对值 0.0128173828 s。两组时间差均通过原目标：

| 时间倍率 | 完整温度场最大差 / °C | 完整含水率场最大差 / (kg/kg) | 临界时长差 / s |
|---|---:|---:|---:|
| 1 至 0.5 | 5.3665812274e-6 | 2.0048838774e-6 | 0.0210571289 |
| 0.5 至 0.25 | 1.3326492478e-6 | 5.1523089262e-7 | 0.0054931641 |

主解 `main-fd65219ad643f710` 接受 14690 步、拒绝 0 步。主方案守恒、非线性、边界重构及事件局部检验通过；基础检查实际通过 17 项。严格达标上端点为 182967.1270751953 s，未舍入全域最大含水率为 0.14999999977184433 kg/kg。详细原始指标在本批 `verification_pending.json` 或最终 `verification.json` 中。

以上为主方案已完成的检验。物性、几何及环境敏感性仍以各自完成的空间、时间、边界和事件验证为准，不能从主方案通过推定对照通过。

## 最后一个时间加密算例的提前执行

主流程使用 `--all --n 12800 --workers 3`。为了提前完成队列末尾的长时间算例，同时用同一求解脚本执行：

```powershell
python -B -X utf8 src/problem-4/solve_problem4.py --case appendix4_mean30_time --n 51200 --factor 0.25 --tail mean30
```

此时最多四个数值进程并行，每个进程仍限制 BLAS 为单线程；未修改求解代码、数值参数、输入或情景定义。该独立调用完成 `appendix4_mean30_time-2a08b62d36e27265`，运行时间约 2157.6 s。随后主流程实际输出 `REUSE appendix4_mean30_time-2a08b62d36e27265`，按既定代码、输入、参数与文件指纹复用完成结果。复现全部结果只需运行主流程命令；提前调度命令仅用于本次排程，不是额外入口脚本或验证依据。

## 长时间运行期间其他目录的变更

全部数值验证通过后，原长时间运行在文件保护检查处停止，尚未发布正式工作簿。前后快照显示第 1 问绘图脚本、第 3 问部分源码及其案例文件发生变化。第四问脚本不向这些目录写入，本次实现也没有修改这些文件；不能据此宣称整个长时间运行期间其他题目目录完全静止。

原始失败报告及完整快照分别保留为 `concurrent_changes_preservation.json`、`concurrent_changes_protected_before.json` 和 `concurrent_changes_protected_after.json`，未覆盖或回退其他目录的当前内容。重新核对题面、附件 1、附件 2 和原 result4 模板，四份文件的运行前、运行后及当前 SHA-256 均一致。第四问求解代码指纹也未变。当前第三问物性公式仍与题面附录 3 一致；本问独立计算所用输入、方程和全部已完成案例没有依赖第三问的生成结果。

随后从当前工作区状态再次执行 `--all --n 12800 --workers 3`，按代码、输入、参数、依赖及数值文件指纹复用已完成案例，并重新执行验证、文件保护及导出。最终 `preservation.json` 的检查区间是这次复验；最初长时间运行的目录变化事实以上述归档报告为准，不用新基线掩盖历史变化。
